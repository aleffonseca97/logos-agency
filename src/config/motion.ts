/**
 * Motion config — fonte canônica em `@/design-system/motion`.
 * Mantido para compatibilidade com imports existentes.
 */
export {
  durations,
  easings,
  effectIntensity,
  motionTransitions,
  motionVariants,
  sectionFadeIn,
  sectionFadeUp,
  sectionStagger,
  sectionViewport,
  type EffectIntensity,
} from "@/design-system/motion";
