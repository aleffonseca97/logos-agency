export interface BrandColors {
  primary: string;
  secondary: string;
  accent: string;
  background: string;
  surface: string;
  white: string;
}

export interface LogosTheme {
  id: string;
  name: string;
  brand: BrandColors;
}
