/**
 * Fonte única de verdade para cores de marca (TypeScript).
 * Mantenha em sincronia com src/styles/themes/*.css
 *
 * Re-exporta de `../colors` para manter um único hex canônico.
 */
export {
  brandColors as logosBrandColors,
  type BrandColorKey,
} from "../colors";
